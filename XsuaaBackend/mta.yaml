_schema-version: 3.3.0
ID: XsuaaBackend
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: XsuaaBackend-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: XsuaaBackend-auth
      - name: XsuaaBackend-db

  - name: XsuaaBackend-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: XsuaaBackend-db

resources:
  - name: XsuaaBackend-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: XsuaaBackend-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'Riskviewer (XsuaaBackend ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Riskviewer'
          - name: 'RiskManager (XsuaaBackend ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.RiskManager'
  - name: XsuaaBackend-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
